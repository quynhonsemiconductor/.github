{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": [
    "Org-wide Renovate preset. Consumed by a one-line renovate.json in each repo:",
    "  { \"extends\": [\"local>quynhonsemiconductor/.github:renovate-config\"] }",
    "",
    "PRIVATE PACKAGE ACCESS IS NOT CONFIGURED HERE, and looking for it in this file",
    "is the obvious mistake. Renovate reads @quynhonsemiconductor/* from GitHub",
    "Packages, which is a private scope its installation token does not cover — it",
    "reported 'no-result' for all five packages and silently proposed no updates",
    "for them. The fix is a HOST RULE bound in the Mend Developer Portal UI",
    "(Settings > Credentials > Host Rules): npm.pkg.github.com, hostType npm,",
    "using the GITHUB_PACKAGES_READ_TOKEN secret — a read:packages classic PAT",
    "belonging to the qnsc-bot machine account.",
    "",
    "A hostRules block WAS tried here first, referencing the secret as",
    "{{ secrets.GITHUB_PACKAGES_READ_TOKEN }}. It did not resolve on the hosted",
    "app — app.renovatebot.com/encrypt now states config-based secrets are",
    "superseded by UI-managed ones. It is removed rather than left in place,",
    "because config that looks load-bearing and is not costs the next reader more",
    "than no config at all.",
    "",
    "Symptom if the token lapses: Dependency Lookup Warnings reappear on the Mend",
    "dashboard, no PRs are opened for the five first-party packages, and nothing",
    "fails."
  ],
  "extends": [
    "config:recommended",
    ":semanticCommits",
    ":dependencyDashboard"
  ],
  "packageRules": [
    {
      "description": "Group non-major npm updates into one PR per dependency type, mirroring the Dependabot policy this replaces. config:recommended separates majors already but does NOT group anything, so without this a handover from Dependabot trades two grouped PRs a week for one PR per dependency.",
      "matchManagers": [
        "npm"
      ],
      "matchUpdateTypes": [
        "minor",
        "patch"
      ],
      "matchDepTypes": [
        "dependencies"
      ],
      "groupName": "npm production (non-major)",
      "labels": [
        "dependencies"
      ]
    },
    {
      "description": "Same, for dev dependencies — kept separate so a dev-tool bump never sits in the same PR as a runtime one.",
      "matchManagers": [
        "npm"
      ],
      "matchUpdateTypes": [
        "minor",
        "patch"
      ],
      "matchDepTypes": [
        "devDependencies"
      ],
      "groupName": "npm development (non-major)",
      "labels": [
        "dependencies"
      ]
    },
    {
      "description": "Majors are labelled and never auto-merged. Separation is already guaranteed: config:recommended sets separateMajorMinor, and the grouping rules above match only minor and patch, so a major can never land inside one of those PRs. This rule deliberately does NOT force groupName off — it would override the shared-CI grouping below, which is intended even for a major of our own actions. Why it matters: four grouped-major PRs were opened across rova and opshub in one week (NestJS 11 to 12 across nine packages plus ioredis 5 to 6; TypeScript 6 to 7 with vitest 4 to 5) and all four were closed unreviewed, because a 45-package diff containing a framework major cannot be attributed when something breaks.",
      "matchUpdateTypes": [
        "major"
      ],
      "automerge": false,
      "labels": [
        "dependencies",
        "major"
      ]
    },
    {
      "description": "Group shared CI (reusables + composite actions) into one PR",
      "matchDepNames": [
        "/^quynhonsemiconductor\\/ci/"
      ],
      "groupName": "shared CI",
      "labels": [
        "ci",
        "shared"
      ]
    },
    {
      "description": "Auto-merge patch bumps of pinned GitHub Actions once checks pass",
      "matchManagers": [
        "github-actions"
      ],
      "matchUpdateTypes": [
        "patch"
      ],
      "automerge": true
    },
    {
      "description": "First-party packages: their own group, never auto-merged. They are published from app-platform by this org and released together, so a bump to them is a platform decision — burying one in a group with a third-party patch loses that signal. THIS RULE MUST STAY LAST. Renovate applies packageRules in order and a later match overwrites an earlier one field by field. While this rule sat FIRST, the generic 'npm production (non-major)' rule above matched the same packages and overwrote its groupName, so @quynhonsemiconductor/identity and platform-cache were both proposed inside a 46-package third-party group in rova and opshub — the exact outcome this rule exists to prevent. It was invisible because the group PR is still opened and still green; only the dashboard's package list shows where they ended up.",
      "matchPackageNames": [
        "/^@quynhonsemiconductor\\//"
      ],
      "groupName": "qnsc platform packages",
      "automerge": false,
      "labels": [
        "dependencies",
        "platform"
      ]
    }
  ],
  "prConcurrentLimit": 5,
  "schedule": [
    "before 9am on monday"
  ]
}
